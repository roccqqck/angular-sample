import { SideMenuDirective } from './side-menu.directive';

describe('SideMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new SideMenuDirective();
    expect(directive).toBeTruthy();
  });
});
