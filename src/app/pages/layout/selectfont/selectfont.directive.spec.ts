import { SelectfontDirective } from './selectfont.directive';

describe('SelectfontDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectfontDirective();
    expect(directive).toBeTruthy();
  });
});
