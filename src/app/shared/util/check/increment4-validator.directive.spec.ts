import { Increment4ValidatorDirective } from './increment4-validator.directive';

describe('Increment4ValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new Increment4ValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
