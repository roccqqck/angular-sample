import { Continuous4ValidatorDirective } from './continuous4-validator.directive';

describe('Continuous4ValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new Continuous4ValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
