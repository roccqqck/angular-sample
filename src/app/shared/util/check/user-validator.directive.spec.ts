import { UserValidatorDirective } from './user-validator.directive';

describe('UserValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UserValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
